# Dev: Vite proxy handles /api/v1 -> localhost:3000, so leave VITE_API_BASE_URL unset (defaults to '/api/v1')
# Prod: set to your production API domain
# VITE_API_BASE_URL=https://platform-api.rx-ted.deno.net
