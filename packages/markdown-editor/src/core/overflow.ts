export interface MarkdownOverflowOptions {
  /** Wrap long code lines to the container width instead of scrolling horizontally. */
  wrapCode?: boolean;
  /** Fit tables inside the container width (fixed layout + wrapped cell text) instead of overflowing. */
  wrapTables?: boolean;
}
